# Trigonum Broker 2.0

Новая реализация Broker с нуля.

## Приложения

- `apps/site` — публичный сайт.
- `apps/onboarding` — регистрация, KYC и первичное пополнение.
- `apps/cabinet` — личный кабинет после активации аккаунта.

## Общие пакеты

- `packages/ui` — переиспользуемые UI-примитивы.
- `packages/brand` — дизайн-токены и фирменные стили.
- `packages/shared` — общие типы и утилиты.
- `packages/api` — единый HTTP/API transport.

## Установка

```bash
npm install
```

## Запуск

Каждое приложение запускается в отдельном терминале из корня проекта:

```bash
npm run dev:site
npm run dev:onboarding
npm run dev:cabinet
```

Dev-серверы слушают все сетевые интерфейсы (`0.0.0.0`), поэтому доступны
с других устройств в той же локальной сети. Текущие адреса этого компьютера:

- site: `http://192.168.10.152:5173`
- onboarding: `http://192.168.10.152:5174`
- cabinet: `http://192.168.10.152:5175`

Локально используйте `http://localhost:5173`, `http://localhost:5174` и
`http://localhost:5175`. LAN IP может измениться после переподключения к сети;
актуальный IPv4 можно посмотреть командой `ipconfig`.

Если Windows при первом запуске запросит доступ через брандмауэр, разрешите его
для частных сетей.

Если нужный порт уже занят, его можно переопределить. Например, для site:

```bash
npm run dev:site -- --port 5180
```

## Сборка

```bash
npm run build
```
